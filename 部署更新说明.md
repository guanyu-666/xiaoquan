# QB手账工作台 · 更新部署操作说明

> ⚠️ **非常重要：更新新版本前，一定要先把旧网页全部数据导出 JSON 备份！防止数据丢失！**

## 一、更新前（在你现在已经部署好的旧网页上）
1. 打开你已经部署好的「QB手账工作台」（旧名「小圈」）网页。
2. 滑到 **设置页顶部的「💾 备份 & 恢复」模块**，点击 **【导出 JSON】**，把备份文件保存到手机 / 电脑本地。

## 二、替换网页文件
3. 把下面迭代改造完成后的新版 `index.html` 下载 / 复制到电脑本地仓库文件夹，替换掉原来的旧 `index.html`。
4. `manifest.json`、`sw.js`、`icons` 图标文件夹 **继续沿用原来的文件，不用改动**（本轮已把 `sw.js` 缓存版本号升到 `qb-shouzhang-v2`，部署后会自动刷新旧缓存；后续想换 PWA 桌面图标，直接替换 `icons` 文件夹里的图片即可）。

## 三、上传并部署
5. 将本地文件夹内全部文件（`index.html` + `manifest.json` + `sw.js` + `icons` 文件夹）全部选中，上传到你的 GitHub 仓库根目录，提交 Commit changes。
6. 等待 GitHub Pages 部署完成（1–3 分钟），访问你的在线网址。

## 四、手机端
7. 把旧的桌面 PWA 图标删除；用浏览器打开新网页，重新 **「添加到主屏幕」** 生成新版独立 PWA（新名称显示为「QB手账工作台」）。
8. 打开新版网页，进入 **设置 → 备份 & 恢复 → 导入恢复**，选择第一步保存的备份 JSON，导入；你的旧纪念日、记账、食谱等全部数据恢复，并可使用全部功能（目标打卡、记录本、书籍、设置）。

---

## 本次更新内容速览（UI 与交互改版）
- 🏷️ **应用更名**：全站名称统一为「QB手账工作台」（页面标题、顶栏、PWA 名称、提示文案、备份文件名均无旧名残留）。
- 🔲 **记录本网格视图**：「灵感记录」和「日记」新增宫格展示模式；列表 ⇄ 宫格通过新增的图标按钮一键切换（▦ / ☰）；宫格按屏幕宽度自适应列数，卡片圆角、间距、配色与现有风格一致；视图偏好分别记忆，刷新或重新进入后保持上次选择。
- 💾 **备份入口收敛**：移除各页面重复的「备份 & 恢复」入口，仅在 **设置页保留一处**，并固定为设置页置顶的第一个模块，原有图标、文案与导出 / 导入 / 复制 / 清空功能不变。
- 🐱 **主页吉祥物升级**：
  - 形象已 **抠图为透明背景**（去除原方形贴图的底色与水印，无方形边框）；
  - 加入 **呼吸浮动循环动画**，并有完整的可点击态（桌面端 hover 放大 + 光标抓手、按下缩小反馈）；
  - 首次进入显示 **「点我试试」引导气泡**，点击一次后永久消失（状态持久化）；
  - **支持手动拖动** 位置（限制在屏幕范围内），拖动后位置自动记忆，刷新后保持；拖动结束不会误触发点击交互，点击夸夸气泡的原有行为保留。
- 💡 以上均为 UI / 交互层改动：数据结构、备份 / 恢复逻辑、其他模块功能完全不受影响（宫格视图偏好与吉祥物位置存于独立 localStorage 键，不进入备份 JSON）。

## 历史更新内容
- 🐱 吉祥物形象为 ins 风软萌小猫（点击夸夸交互保留）。
- 🎯 **目标打卡**：自定义卡片（名称 / 颜色 / 备注 / **单位**）；卡片上「本次完成量」旁的单位可点击修改（常用单位 + 自定义）；打卡记录与本周 / 本月 / 累计进度均带单位显示。
- 📓 **记录本**：灵感记录 + 日记双分栏；日记支持自定义封面图、**正文图文混排（插入图片）**、查看 / 编辑 / 删除。
- 🗂️ **文件夹分组**（灵感 & 日记共用）：可新建文件夹（名称 / 封面：本地上传或 6 款预设图 / 主题颜色：取色器或预设色板）；支持按文件夹筛选查看，保留「全部」与「未分组」视图；条目可 **单个「归类」或「批量」移入 / 移出**文件夹；文件夹名称 / 封面 / 颜色可随时编辑；**删除文件夹只删分组，其中内容自动移入「未分组」，绝不丢失**，删除前有二次确认并显示将受影响的内容条数。文件夹内条数、当前选中文件夹、空文件夹状态均有清晰提示，新建条目会自动归入当前所在文件夹。
- 🍰 **食谱图鉴**：内置图鉴 + 自定义食物；点击食物图片可 **预览大图 / 替换图片 / 编辑（名称·分类·图片）/ 删除**（也可覆盖图鉴里的默认食物）。
- 📚 **书籍**：书单（书名 / 作者 / 简介 / 标签 / 状态 / 感想，可增删改）+ **本地 TXT 阅读器**（仅读取本机上传的文件，不联网下载，支持翻页、字号调节、阅读进度自动保存与恢复）。
- 🎨 **设置中心**：主题色（预设 + 取色器）、全局字号、字体颜色，存于 `DB.ui`，实时生效，可一键恢复默认。
- 🔗 底部 tab 顺序：主页 → 目标 → 记录 → 记账 → 食谱 → 书籍 → 设置（已移除原「计划」模块）。
- 💾 导出 JSON 自动包含全部数据（含 `nbFolders` 文件夹）；导入兼容新旧备份；所有删除操作保留二次确认弹窗。
- 🛡️ PWA / service-worker / 返回键路由防护 / 桌面安装逻辑全部原样保留。
